# Copy to .env and paste your real token (https://dashboard.ngrok.com/get-started/your-authtoken)
# The exact variable name NGROK_AUTHTOKEN is required — the ngrok v3 agent reads it.
NGROK_AUTHTOKEN=your-token-here

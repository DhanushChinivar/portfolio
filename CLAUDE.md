# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This is a new, currently empty development workspace. No project has been scaffolded yet — there are no build, lint, or test commands to document.

## When code is added

Update this file as the project takes shape:

- Add the build, lint, test, and run commands once a toolchain is chosen (including how to run a single test).
- Document the high-level architecture once there is enough structure that it can't be inferred from a single file.
- Initialize git (`git init`) before substantial work begins so changes are tracked.
